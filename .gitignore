# dependencies
node_modules
.pnpm-store

# build outputs
dist
.next
.turbo
out

# env files
.env
.env.local
.env.*.local
# 中心化凭证（位于 ~/.investdojo.env，默认在工作区外；这里加一道双保险）
.investdojo.env
investdojo.env

# platform builds
src-tauri/target
android
ios

# misc
.DS_Store
*.tsbuildinfo

# devcloud runtime
logs/
celerybeat-schedule
python-services/.venv/
python-services/.task_history/
apps/web/.next/
__pycache__/
*.pyc

# agent / working memory (not project source)
.codebuddy/
