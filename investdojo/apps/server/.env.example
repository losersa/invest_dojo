# Server
PORT=4000

# ⚠️  外部 API 凭证已集中管理在 ~/.investdojo.env （不进 git）
# 服务启动时会自动读取，这里只配项目内部变量

# DeepSeek API (留空则使用 mock 数据)
DEEPSEEK_API_KEY=

# Supabase (服务端使用 service_role key)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Redis (可选, 用于缓存)
REDIS_URL=
