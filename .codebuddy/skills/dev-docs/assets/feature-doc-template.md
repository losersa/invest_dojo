# <功能名称>

> 创建：YYYY-MM-DD · 状态：✅ 已上线 / 🔶 开发中
> 变更日志：见 `docs/ops/change-log.md`（搜本功能名）

## 功能概述

<一段话说明这个功能解决什么问题、给谁用>

## 使用入口

- 页面：`http://localhost:3000/<路径>`（可见性：公开 / 仅内部员工）
- API：`<METHOD> <路径>`（所属服务 <svc 名>，端口 <port>）

## 架构与数据流

<前端 → 网关/代理 → 微服务 → DB/外部依赖 的调用链，1 张文字流程或列表>

- 前端：`apps/web/src/app/...`
- 后端：`python-services/<svc>/...`
- 数据表：`<表名>`（迁移文件 `migrations/xxx.sql`）

## API 清单

| 方法 | 路径 | 功能 | 鉴权 |
|---|---|---|---|
| GET | /api/v1/... | ... | 无 / X-User-Id + 角色 |

## 配置项

<环境变量 / 常量及默认值；无则写"无">

## 注意事项（已知坑）

<运维和使用中已知的坑：重启步骤、性能边界、数据前提；每条一句话，详版链到 dev-troubleshooting.md 的章节号>

## 变更历史

- YYYY-MM-DD：初版上线
